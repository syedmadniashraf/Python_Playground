# 4. Replace the double space from problem 3 with single spaces.

name = "Madni is a  good  boy"

print(name.replace("  "," "))# Strings are immutable which means you cannot change them by running functions on them.
